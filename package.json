{
  "name": "jumpgate-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build -w packages/jumpgate && npm run build -w packages/jumpgate-vscode",
    "test": "npm run test -w packages/jumpgate",
    "lint": "eslint packages/*/src/"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "typescript-eslint": "^8.0.0"
  }
}
