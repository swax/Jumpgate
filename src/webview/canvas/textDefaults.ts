/** Minimum resolution for text rendering. Higher values produce crisper text when zoomed out. */
export const MIN_TEXT_RESOLUTION = 4;

/** Base font size used for node and edge labels. */
export const BASE_FONT_SIZE = 16;
